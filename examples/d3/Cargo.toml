[package]
name = "d3"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
web_modules = { path = "../..", features = ["dev"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
