{
  "name": "lit-element-example-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Type-check (tsc --noEmit) and end-to-end (Playwright) tests for the web-modules Lit + Bootstrap example. The example itself needs no Node — this is test tooling only.",
  "scripts": {
    "test:types": "tsc --noEmit",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/bootstrap": "^5.2.10",
    "bootstrap": "^5.3.3",
    "lit": "^3.2.0",
    "typescript": "^5.7.0"
  }
}
