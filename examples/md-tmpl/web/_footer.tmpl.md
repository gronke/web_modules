---
name: footer
description: Shared page footer — `_`-prefixed, so it is include-only and never a page.
params:
  - source = str
---

Rendered from `{{ source }}.tmpl.md` by [web_modules](https://github.com/gronke/web_modules)
with [md-tmpl](https://docs.rs/md-tmpl).
